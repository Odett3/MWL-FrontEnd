export const selectToken = (reduxState) => reduxState.user.token;
export const selectUser = (reduxState) => reduxState.user;
export const selectUserListings = (reduxState) => reduxState.user.listings;
